package za.ac.cput.factory;
/*
Author:
Imaan Sadien - 221752838
GitHub Repo:
https://github.com/Elentiya10/EcommercePCParts
 */

import za.ac.cput.domain.*;
import za.ac.cput.util.Helper;

public class DeliveryFactory {
    /*


    public static Delivery buildDelivery(String deliveryId, Customer customer, Order order, Payment payment, String transitDate, String deliveryDate){
        if(Helper.isNullOrEmpty(deliveryId) ||
                Helper.isNullOrEmpty(customer.getCustomerId()) || Helper.isNullOrEmpty(customer.getFirstName()) ||
                Helper.isNullOrEmpty(customer.getLastName()) || Helper.isNullOrEmpty(customer.getPassword())||
                Helper.isNullOrEmpty(order.getOrderId()) || Helper.isNullOrZero(order.getOverallPrice()))
        return null;
        */


    }

